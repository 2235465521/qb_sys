# 交接文档 / Handoff Document

## 1. 任务背景 (Task Context)
本阶段工作针对项目整体架构进行了 Code Review 检查，彻底解决了**“浙江省标准化研究院”**（统一社会信用代码：`12330000470030212Y`）等多挂牌机构在企业列表卡片与右侧详情抽屉中标准数量统计不一致、漏算团标/国标/地标的问题，修复了部署构建时的 TypeScript 编译错误，并已将最新完整代码推送到 GitHub 远程仓库 `main` 分支。

---

## 2. 核心结论与技术成果 (Key Achievements & Findings)

### (1) “浙江省标准化研究院”多 Unit/别名穿透统计与口径对齐
* **痛点根因**：原列表接口使用单一字面量 `WHERE u.unit_name = %s` 精确匹配，且缓存了未穿透的旧数据，导致卡片显示 `0` 或未去重的旧数值。
* **重构方案**：按 **Codebase Design** 原则构建了 `FederatedStandardService` 深度服务模块，支持 **核心机构 (`core`)** 与 **全量扩展 (`expanded`)** 双重统计口径：
  * **核心机构口径 (`scope=core`)**：统计主机构 3 个 `unit_id`，去重后标准数 **417** 项（团标 260 项）。
  * **全量扩展口径 (`scope=expanded`)**：穿透关联下属挂牌中心（金砖研究中心、物品编码中心等 17 个 `unit_id`），去重后标准数 **718** 项（推荐国标 371 项、团标 283 项、地标 43 项）。
* **卡片与抽屉对齐**：重构了 `CompanyBulkListSerializer` 与 `CompanyListSerializer`，搜索列表卡片左侧数字与右侧抽屉数字实现了 **100% 精准对齐**。

### (2) 前端交互与平台差异化说明 (UI/UX)
* **抽屉卡片升级**：在 `StandardDrawer.tsx` 顶部增加了统一社会信用代码、国标/团标/地标分类徽章与全量扩展/核心机构 Radio 切换器。
* **口径提示 Tooltip**：增加了说明浮层，向用户与同事解释本系统基于 `std_id` 的严格去重规则，消除了与企查查/天眼查等第三方商业平台未去重数据的统计疑问。

### (3) 部署构建 Bug 诊断与修复 (Diagnosing Bugs)
* **问题定位**：服务器部署 `npm run build` (`tsc -b`) 时报 6 处 TypeScript 错误。
* **修复内容**：
  * 在 `src/types/index.ts` 中补全了 `SmsTask` 接口缺失的 `target_company`、`scheduled_time`、`started_at`、`finished_at` 属性。
  * 修正了 `SmsTasks/index.tsx` 中 Ant Design `<Tag>` 标签非法的 `size="small"` 属性。
* **验证结果**：`npm run build` 0 errors 打包成功。

---

## 3. 修改的关键文件路径 (Modified Files Overview)

* **后端逻辑与服务**：
  * [companies/services.py](file:///e:/企标管理系统_企标记录/emis/backend/companies/services.py#L705-L950) — 新增 `FederatedStandardService` 深度模块。
  * [companies/serializers.py](file:///e:/企标管理系统_企标记录/emis/backend/companies/serializers.py#L30-L115) — 批量列表序列化器全面接入 `FederatedStandardService` 并发缓存。
  * [companies/views/standard_sync_views.py](file:///e:/企标管理系统_企标记录/emis/backend/companies/views/standard_sync_views.py#L43-L55) — 重构联邦查询 API，支持 `scope` 参数。
  * [standards/models.py](file:///e:/企标管理系统_企标记录/emis/backend/standards/models.py#L225-L255) — 重构计数更新与 `_invalidate_search_cache` 缓存清理。
  * [scripts/populate_standards_count.py](file:///e:/企标管理系统_企标记录/emis/backend/scripts/populate_standards_count.py) — 脚本更新并重新刷新了数据库 849 家企业存量。

* **前端 API 与视图组件**：
  * [src/hooks/useSearchData.ts](file:///e:/企标管理系统_企标记录/emis/frontend/src/hooks/useSearchData.ts#L26-L45) — 增加 `scope` 传参支持。
  * [src/pages/Client/Search/components/StandardDrawer.tsx](file:///e:/企标管理系统_企标记录/emis/frontend/src/pages/Client/Search/components/StandardDrawer.tsx#L215-L255) — 新增信用代码、口径切换器与维度徽章。
  * [src/types/index.ts](file:///e:/企标管理系统_企标记录/emis/frontend/src/types/index.ts#L152-L165) — 补全 `SmsTask` 类型定义。
  * [src/pages/Admin/SmsTasks/index.tsx](file:///e:/企标管理系统_企标记录/emis/frontend/src/pages/Admin/SmsTasks/index.tsx) — 修正 Tag 属性。

---

## 4. GitHub 状态与部署提示 (Git & Deployment Status)

* **Git 提交信息**：
  * `cf7fa6d` — `fix: align company list serializer standards_count with FederatedStandardService`
  * `9b823e6` — `fix: resolve TypeScript interface mismatch and Tag prop error in SmsTasks`
  * `8360f2b` — `feat: refactor federated standard service, add multi-unit penetration and credit code standard breakdown`
* **最新代码已推送到**：`git@github.com:2235465521/qb_sys.git` (`main` 分支)。

### 服务器线上更新命令：
```bash
# 1. 代码拉取与前端打包
cd ~/emis_v2/xiangmu
git pull
cd emis/frontend && npm run build

# 2. 后端平滑重载
cd ~/emis_v2/xiangmu
pm2 reload ecosystem.config.js
```

---

## 5. 建议技能与后续工作 (Suggested Skills & Next Steps)

### Suggested Skills
* `code-review`: 用于后续新增功能时的代码规范与逻辑审查。
* `diagnosing-bugs`: 用于后续生产环境可能遇到的跨库数据调取或缓存问题定位。
* `domain-modeling`: 用于企标/团标/国标数据模型及更多企业信用代码同义词字典的扩展。

### 后续参考操作
若后续需要针对其他特定大型科研院所（如“中国标准化研究院”、“中国信息通信研究院”等）配置专属的别名/下属挂牌穿透规则，可在 [services.py](file:///e:/企标管理系统_企标记录/emis/backend/companies/services.py) 的 `FederatedStandardService.get_unit_ids_by_company` 中扩展配置映射。
