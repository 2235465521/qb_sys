企业管理信息系统 (EMIS) 技术开发与协同规范

1. 技术栈选型 (方案 A)
   前端: React 18 + Vite + TypeScript + React Router v6

UI 框架: Ant Design (适合企业级后台和复杂数据展示)

状态/请求: React Query (处理状态缓存) + Axios

后端: Django 5.x + Django REST Framework (DRF)

异步任务: Celery + Redis (处理每日短信、后台解析、打包下载)

数据库: MySQL 8.x

部署环境: 阿里云 ECS + Nginx + Docker Compose

2. 代码开发强制纪律 (AI 与协同开发者必读)
   为了保证代码的可读性和可维护性，所有提交的代码必须严格遵守以下规范：

2.1 前端模块化纪律 (单文件 < 250 行原则)
禁止将整个页面写在一个 .tsx 文件中。必须按照 “页面组件化” 拆分。

错误做法： CompanyList.tsx (800行，包含搜索表单、表格、分页、弹窗)。

正确做法：

CompanyList/index.tsx (页面入口，仅负责组合，约 50 行)

CompanyList/components/SearchForm.tsx (独立的高级查询表单，约 150 行)

CompanyList/components/DataTable.tsx (数据表格组件，约 150 行)

CompanyList/components/ActionModal.tsx (增删改弹窗，约 100 行)

hooks/useCompanyData.ts (抽离数据请求逻辑，约 80 行)

2.2 后端解耦纪律 (App 与路由分离)
禁止在 views.py 或 models.py 中堆积所有代码。

按业务拆分 App： 至少拆分为 users (用户/会员), companies (企业信息), standards (标准与解析), notifications (短信通知) 四个独立的 Django App。

胖 Model，瘦 View： 复杂的业务逻辑（如国标引用次数+1的事务处理）写在 Model 的方法或专门的 services.py 层中，View 层只负责接收请求和返回响应。

视图层拆分： 如果一个 App 的视图超过 300 行，将 views.py 改为一个包目录 views/，按接口拆分如 views/company_views.py 和 views/export_views.py。

3. 文件存储与优化策略 (核心需求)
   为了应对未来海量的标准文件，优化查询和下载效率，静态文件与媒体文件必须严格分区。

3.1 目录划分
图片存储: /media/images/ (如企业Logo、系统图标，平铺存储或按业务类型分子目录)。

PDF/文档存储: 强制采用按日期时间分层存储。路径格式：/media/pdfs/YYYY/MM/DD/。

3.2 Django Model 定义规范 (示例)
Python

# standards/models.py

def pdf_upload_path(instance, filename): # 根据当前日期自动生成按年/月/日划分的路径 # 例如: media/pdfs/2026/05/15/standard_xQ_123.pdf
return f'pdfs/{instance.upload_date.strftime("%Y/%m/%d")}/{filename}'

class StandardDocument(models.Model):
standard_no = models.CharField(max_length=100, db_index=True) # 标准号必须加索引
pdf_file = models.FileField(upload_to=pdf_upload_path)
upload_date = models.DateField(auto_now_add=True) # ... 其他字段
优化原理： 单个文件夹下文件数量过多会导致操作系统的 I/O 检索极速下降。按天分卷存储可以保证每个文件夹下的文件数量在安全阈值内，极大地提升 Nginx 寻址和打包下载的速度。

4. 系统目录架构总览
   4.1 前端 (React + Vite)
   Plaintext
   frontend/
   ├── package.json
   ├── vite.config.ts
   └── src/
   ├── api/ # Axios 实例及所有后端接口请求定义
   ├── assets/ # 静态资源 (图片、CSS)
   ├── components/ # 全局公共组件 (如通用表格、高级查询面板)
   ├── hooks/ # 自定义 React Hooks (包含业务逻辑复用)
   ├── layouts/ # 页面布局 (Sidebar, Header)
   ├── pages/ # 页面视图 (按照业务模块划分子目录)
   │ ├── Admin/ # 后台管理页面群
   │ └── Client/ # 前台三大模块页面群
   ├── store/ # 全局状态管理 (如需要)
   └── utils/ # 工具函数 (日期格式化、特殊标准号正则处理)
   4.2 后端 (Django)
   Plaintext
   backend/
   ├── manage.py
   ├── requirements.txt
   ├── config/ # Django 主配置 (settings.py, urls.py)
   ├── apps/ # 业务模块 (使用模块化解耦)
   │ ├── users/ # 会员、权限模块
   │ ├── companies/ # 企业信息增删改查
   │ ├── standards/ # 企标/团标管理、规范解析模块
   │ └── notifications/ # 短信发送、定时任务模块
   └── media/ # 挂载的云盘目录 (不在代码仓库中)
   ├── images/
   └── pdfs/
   └── 2026/
   └── 05/
   └── 15/
5. 阿里云部署与 Nginx 配置规范
   在阿里云 ECS 上，采用 Docker Compose 进行一键部署，并由主机上的 Nginx 进行反向代理和静态资源加速。

5.1 Nginx 核心配置 (性能优化版)
对于媒体文件（特别是按日期存储的 PDF），必须交由 Nginx 直接返回，绝对不能让 Django 去读取文件流，否则会严重拖垮应用服务器。

Nginx
server {
listen 80;
server_name your_domain.com; # 替换为您的阿里云域名或IP

    # 1. 前端 React 静态文件服务
    location / {
        root /usr/share/nginx/html/frontend_dist;
        index index.html;
        try_files $uri $uri/ /index.html; # 解决 React Router 刷新 404 问题
    }

    # 2. 后端 Django API 反向代理
    location /api/ {
        proxy_pass http://127.0.0.1:8000; # 代理到 Django 容器
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # 3. 图片文件直接由 Nginx 代理 (高性能)
    location /media/images/ {
        alias /var/www/emis/media/images/;
        expires 30d; # 开启浏览器缓存
    }

    # 4. PDF 文件下载优化 (按日期目录读取)
    location /media/pdfs/ {
        alias /var/www/emis/media/pdfs/;

        # 针对大文件下载的优化
        sendfile on;
        tcp_nopush on;
        tcp_nodelay on;

        # 强制触发下载而不是在浏览器中预览 (可选)
        # add_header Content-Disposition 'attachment;';

        # 仅限登录用户下载的高级安全玩法 (X-Accel-Redirect)
        # 建议后期实现：由 Django 验证权限后，返回空响应头 X-Accel-Redirect 丢给 Nginx 发送文件
        internal; # 如果开启 X-Accel-Redirect，取消注释此行
    }

}
